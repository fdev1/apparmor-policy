#include <tunables/global>

profile djmount /usr/bin/djmount {
	#include <abstractions/base>
	#include <abstractions/consoles>

	capability sys_nice,
	capability sys_admin,

	/upnp/ rw,
	/dev/fuse rw,
	/usr/bin/djmount mr,
	/usr/bin/fusermount ix,
	/etc/fuse.conf r,
}
